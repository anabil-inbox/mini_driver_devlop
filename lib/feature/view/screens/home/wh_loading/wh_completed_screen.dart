import 'package:flutter/material.dart';

class WHCompletedScreen extends StatelessWidget {
  const WHCompletedScreen({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      
    );
  }
}